from dpipe.split import train_val_test_split


n_splits = 3
val_size = 5
n_experiments = 1

# split = train_val_test_split( ids=dataset.df.index.tolist(), n_splits=n_splits, val_size=val_size,
#                               random_state=random_state)[:n_experiments]

split = train_val_test_split( ids=dataset.ids, n_splits=n_splits, val_size=val_size,
                              random_state=random_state)[:n_experiments]
