from amid.lits import LiTS
from connectome import Filter

from ood.dataset.lits import CanonicalCTOrientation
from ood.paths import LITS_DATA_PATH


dataset_raw = LiTS(root=LITS_DATA_PATH) >> Filter(lambda fold: fold != 'test') >> CanonicalCTOrientation()
