# from functools import partial
#
# from ood.dataset.utils import scale_ct as scale_ct_init
#
#
# voxel_spacing = (1.5, 1.5, 1.5)
# n_tumors = np.ones(len(train_ids))
# scale_ct = partial(scale_ct_init, min_value=-1000)
